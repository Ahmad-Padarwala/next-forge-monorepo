{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "dev": {
      "cache": true,
      "persistent": true
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "dist/**"]
    },
    "lint": {},
    "format": {}
  }
}
